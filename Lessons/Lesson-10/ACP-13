decimal = int(input("Enter a number to convert to binary:"))
binary = ""
if decimal == 0:
    binary = "0"
else:
    while decimal > 0:
        remainder = decimal % 2  
        binary = remainder + binary 
        decimal //= 2
print(decimal,"in binary is:",binary)
